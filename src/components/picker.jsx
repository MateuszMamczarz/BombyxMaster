import React from 'react';

const Picker = ({ options, onPick }) => {
    return (
        <div className='picker-container'>
            {/* {options.map(options)} */}
        </div>
    )
}

export default Picker;